diff --git a/src/flush-instruction-cache.cc b/src/flush-instruction-cache.cc
index 5ec2dc1..8414df4 100644
--- a/src/flush-instruction-cache.cc
+++ b/src/flush-instruction-cache.cc
@@ -12,7 +12,7 @@ namespace v8 {
 namespace internal {
 
 void FlushInstructionCache(void* start, size_t size) {
-  if (size == 0) return;
+  if (FLAG_jitless) return;
 
 #if defined(USE_SIMULATOR)
   base::MutexGuard lock_guard(Simulator::i_cache_mutex());
