// Basename vendor.mjs is preserved across reloadApplication so webpack
// isolate-reload can keep @angular/core in the original realm.
export const n = (globalThis.__tnsReloadVendorEvals = (globalThis.__tnsReloadVendorEvals || 0) + 1);
